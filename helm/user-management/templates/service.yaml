apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-management.fullname" . }}
  labels:
    app: {{ include "user-management.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "user-management.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
